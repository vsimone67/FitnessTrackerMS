version: '3.4'
services:
    workoutservice:
        ports:
            - '4300:80'
        volumes:
            - './dockerlogs:/logs'
            - './sharedsettings:/appsettings'
    dietservice:
        ports:
            - '4301:80'
        volumes:
            - './dockerlogs:/logs'
            - './sharedsettings:/appsettings'
    signalrhub:
        ports:
            - '4302:80'
        volumes:
            - './dockerlogs:/logs'
            - './sharedsettings:/appsettings'
    webstatus:
        ports:
            - '4303:80'
        volumes:
            - './dockerlogs:/logs'
            - './sharedsettings:/appsettings'